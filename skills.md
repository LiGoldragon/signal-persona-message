# skills — signal-message

Read `ARCHITECTURE.md` before editing this repository.

Keep `ethos/signal.ethos` as the sole authored Interface source. Add or
change Types through the authority manifest and strict generation path. Opaque
identity seats are minted once and recorded; they are never derived from a
spelling, position, or content digest.

Keep the four Interface role lists empty until Logos owns behavior. Seat
`Input`, `Output`, structural archive behavior, Dotos behavior, and frame
routing explicitly in `src/schema/lib/behavior.rs` during this bootstrap
stage. Do not add visible Rust aliases for encoded Types.

Every Type or role change needs a focused witness, all route families need
frame round trips, and the complete gate is the exact Nix flake check documented
in the README.
